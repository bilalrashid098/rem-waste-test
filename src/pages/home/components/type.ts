/**
 * Props for the ProductLoader component.
 */
export interface ProductLoaderProps {
  /**
   * Specifies the number of shimmer product cards to render.
   * 
   * Default is 10.
   * 
   * Example: 5, 10, 15
   */
  length: number;
}
